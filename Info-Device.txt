Depending on your application's configuration some manual setup may be required:

  1. Ensure you have defined default url options in your environments files. Here
     is an example of default_url_options appropriate for a development environment
     in config/environments/development.rb:

       config.action_mailer.default_url_options = { host: 'localhost', port: 3000 }

     In production, :host should be set to the actual host of your application.

     * Required for all applications. *

  2. Ensure you have defined root_url to *something* in your config/routes.rb.
     For example:

       root to: "home#index"
     
     * Not required for API-only Applications *

  3. Ensure you have flash messages in app/views/layouts/application.html.erb.
     For example:

       <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>

     * Not required for API-only Applications *

  4. You can copy Devise views (for customization) to your app by running:

       rails g devise:views
       
     * Not required *
______________________________


Dependiendo de la configuración de tu aplicación, es posible que se requiera una configuración manual:

1. Asegúrate de haber definido las opciones de URL predeterminadas en tus archivos de entorno. Aquí hay un ejemplo de opciones de URL predeterminadas apropiadas para un entorno de desarrollo en config/environments/development.rb:

config.action_mailer.default_url_options = { host: 'localhost', port: 3000 }

En producción, :host debe establecerse en el host real de tu aplicación.

Requerido para todas las aplicaciones. *

2. Asegúrate de haber definido root_url a algo en tu archivo de configuración config/routes.rb. Por ejemplo:

root to: "home#index"

No requerido para aplicaciones solo de API *

3. Asegúrate de tener mensajes flash en app/views/layouts/application.html.erb. Por ejemplo:

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
No requerido para aplicaciones solo de API *

4. Puedes copiar las vistas de Devise (para personalización) en tu aplicación ejecutando:

rails g devise:views

No requerido *
